import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Barphase — Proprietary Trading OS",
  description:
    "Barphase proprietary futures trading operating system with real-time price action analysis, liquidity sweeps, BOS/CHOCH detection, order blocks, fair value gaps, trade journal, performance analytics, and multi-instrument scanning.",
  icons: {
    icon: "/favicon.ico",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
